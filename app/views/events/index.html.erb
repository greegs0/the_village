<%# Navbar %>
<%= render 'shared/navbar' %>

<div class="container-fluid px-4 px-lg-5 py-4">
  <!-- Hero Section -->
  <div class="text-center mb-5">
    <div class="fs-1 mb-3">🌍</div>
    <h1 class="display-5 fw-normal mb-3">Événements Communautaires</h1>
    <p class="lead text-secondary mb-4">
      Découvrez et participez aux événements organisés dans votre région. Rencontrez vos voisins et créez des liens !
    </p>
    <button type="button" class="btn btn-dark btn-lg" data-bs-toggle="modal" data-bs-target="#createCommunityEventModal">
      <span class="me-2">+</span>Créer un événement dans votre région
    </button>
  </div>

  <!-- View Toggle -->
  <ul class="nav nav-pills bg-light rounded-3 p-1 mb-4 justify-content-center" style="max-width: 400px; margin: 0 auto;">
    <li class="nav-item flex-fill" role="presentation">
      <button class="nav-link active w-100" data-bs-toggle="pill" data-bs-target="#listView" type="button">
        📋 Liste
      </button>
    </li>
    <li class="nav-item flex-fill" role="presentation">
      <button class="nav-link w-100" data-bs-toggle="pill" data-bs-target="#mapView" type="button">
        🗺️ Carte
      </button>
    </li>
  </ul>

  <!-- Tab Content -->
  <div class="tab-content">
    <div class="tab-pane fade show active" id="listView">
      <!-- Events Grid -->
      <div class="row g-4">
        <% @events.each do |event| %>
          <%= render "event_card", event: event %>
        <% end %>
      </div>
    </div>

    <div class="tab-pane fade" id="mapView">
      <p class="text-center text-muted py-5">Vue carte à venir</p>
    </div>
  </div>
</div>

<!-- Modal Créer un événement communautaire -->
<div class="modal fade" id="createCommunityEventModal" tabindex="-1" aria-labelledby="createCommunityEventModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content rounded-4">
      <div class="modal-header border-0 pb-0">
        <div>
          <h5 class="modal-title fw-medium" id="createCommunityEventModalLabel">Créer un événement communautaire</h5>
          <p class="text-secondary small mb-0">Organisez un événement et invitez vos voisins à participer</p>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body pt-3">

  <!-- Form -->
        <%= simple_form_for @event do |f| %>
          <%= f.error_notification %>

          <!-- Titre de l'événement -->
          <div class="mb-3">
            <%= f.input :name,
                        label: "Titre de l'événement",
                        required: true,
                        input_html: {
                          class: "form-control",
                          placeholder: "Ex: Apéro voisins"
                        } %>
          </div>

          <!-- Date et Heure -->
          <div class="row g-3 mb-3">
            <%= f.input :date,
                        label: "Date",
                        required: true,
                        input_html: {
                          class: "form-control",
                          placeholder: "jj/mm/aaaa"
                        } %>
          </div>

          <!-- Lieu-->
          <div class="mb-3">
            <%= f.input :place,
                        label: "Lieu",
                        required: true,
                        input_html: {
                          class: "form-control",
                          placeholder: "Ex : Parc du quartier"
                        } %>
          </div>

          <!-- Catégorie-->
          <div class="mb-3">
            <%= f.input :category,
                        as: :select,
                        collection: [
                          ['Social', 'social'],
                          ['Sport', 'sport'],
                          ['Culture', 'culture'],
                          ['Famille', 'famille'],
                          ['Jardinage', 'jardinage'],
                          ['Autre', 'autre']
                        ],
                        label: "Catégorie",
                        required: true,
                        prompt: "Sélectionner une catégorie",
                        input_html: {
                          class: "form-select"
                        },
                        label_html: {
                          class: "form-label fw-medium"
                        } %>
          </div>

          <!-- Description -->
          <div class="mb-3">
            <%= f.input :description,
                        as: :text,
                        label: "Description",
                        required: true,
                        input_html: {
                          class: "form-control",
                          rows: 4,
                          placeholder: "Ex : Décrivez votre événement..."
                        } %>
          </div>

          <!-- Nombre maximum de participants (optionnel) -->
          <div class="mb-3">
            <%= f.input :max_participations,
                        label: "Nombre maximum de participants (optionnel)",
                        required: false,
                        input_html: {
                          class: "form-control",
                          placeholder: "Ex : 20"
                        } %>
          </div>

          <div class="d-flex justify-content-end gap-2">
            <%= f.button :button, "Annuler", class: "btn btn-outline-secondary", data: { bs_dismiss: "modal" } %>
            <%= f.button :submit, "Créer l'événement", class: "btn btn-dark" %>
          </div>
        <% end %>

        <form>
      </div>
    </div>
  </div>
</div>

